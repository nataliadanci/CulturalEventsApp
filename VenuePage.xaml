<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CulturalEventsApp.VenuePage"
             Title="Detalii locație">
    <ContentPage.Content>
        <StackLayout Margin="20">
            <Editor Placeholder="Introduceți numele locației" Margin="20"
                     Text="{Binding VenueName}"
                     HeightRequest="50" />
            <Editor Placeholder="Introduceți adresa" Margin="20"
                    Text="{Binding Adress}"
                    HeightRequest="50" />

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Text="Salvează" Grid.Column="0" MaximumWidthRequest="200"
                        Clicked="OnSaveButtonClicked" />
                <Button Text="Show on Map" Grid.Column="1" MaximumWidthRequest="200"
                        Clicked="OnShowMapButtonClicked" />
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>

