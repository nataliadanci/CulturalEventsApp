<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CulturalEventsApp.VenueEntryPage"
             Title="Locații">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Adaugă locație"
                     Clicked="OnVenueAddedClicked" />
    </ContentPage.ToolbarItems>

    <ListView x:Name="listView"
          Margin="20"
          ItemSelected="OnListViewItemSelected">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Grid Padding="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <!-- Detalii Locație -->
                        <Frame CornerRadius="10" BorderColor="Black" Margin="0,10" BackgroundColor="Gray" WidthRequest="300">
                            <StackLayout Orientation="Vertical" Padding="15">
                                <Label Text="{Binding VenueName}"
                                        FontSize="18"
                                        FontAttributes="Bold"
                                        TextColor="LightPink"
                                        Padding="10,0,10,10"/>
                                <Label Text="{Binding Adress}"
                                       FontSize="14"
                                       TextColor="Black"
                                       Padding="10,0,0,0"/>
                            </StackLayout>
                        </Frame>

                        <!-- Buton Ștergere (Vizibil doar la hover) -->
                        <Button Text="🗑️"
                            Grid.Column="1"
                            Command="{Binding DeleteCommand}"
                            CommandParameter="{Binding}"
                            BackgroundColor="Transparent"
                            TextColor="Red"
                            IsVisible="False">
                        </Button>

                        <!-- Vizibilitate la hover -->
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Property="IsVisible" Value="True" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>